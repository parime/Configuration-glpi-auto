name: "Continuous Integration"

on:
  push:
    branches:
      - "main"
      - "dev"
      - "release/**"
    tags:
      - "*"
  pull_request:
    branches:
      - "main"
      - "dev"
      - "release/**"
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: true

# Principe du moindre privilege (alerte CodeQL actions/missing-workflow-permissions) : sans bloc
# explicite, GITHUB_TOKEN recoit les permissions par defaut du depot (souvent plus large que
# necessaire). Aucun job de ce workflow n'ecrit quoi que ce soit (checkout, analyse, tests), un
# seul bloc en lecture seule au niveau du workflow suffit pour tous.
permissions:
  contents: read

jobs:
  generate-ci-matrix:
    name: "Generate CI matrix"
    uses: "glpi-project/plugin-ci-workflows/.github/workflows/generate-ci-matrix.yml@v1"
    with:
      glpi-version: "11.0.x"

  ci:
    name: "GLPI ${{ matrix.glpi-version }} - php:${{ matrix.php-version }} - ${{ matrix.db-image }}"
    needs: "generate-ci-matrix"
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.generate-ci-matrix.outputs.matrix) }}
    uses: "glpi-project/plugin-ci-workflows/.github/workflows/continuous-integration.yml@v1"
    with:
      plugin-key: "configurationglpiauto"
      glpi-version: "${{ matrix.glpi-version }}"
      php-version: "${{ matrix.php-version }}"
      db-image: "${{ matrix.db-image }}"

  phpstan:
    name: "PHPStan Analysis"
    runs-on: ubuntu-latest
    needs: ci
    # #274: PHPStan a besoin du VRAI autoload GLPI (.phpstan-bootstrap.php charge
    # /var/www/glpi/vendor/autoload.php) pour resoudre les classes du coeur (CommonDBTM, Session...)
    # utilisees par src/ -- sans ca il tourne "a l'aveugle" et remonte des milliers de faux
    # "class.notFound". Un simple `actions/checkout` + `composer install` sur ubuntu-latest nu (la
    # version precedente de ce job) ne fournit jamais ce chemin : confirme en CI sur la PR #274 (0
    # erreur en local dans le conteneur Docker de dev, 1000+ en CI). Ce job construit donc son
    # propre GLPI reel via Docker et lance PHPStan a l'interieur via `docker exec`, exactement la
    # meme recette que le plugin jumeau assetsign-glpi (`.github/workflows/ci.yml`), qui l'a
    # eprouvee la premiere.
    env:
      GLPI_IMAGE: glpi/glpi:11.0.8

    steps:
      - name: "Checkout code"
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7 (SHA pinned, cf. Semgrep github-actions-mutable-action-tag)

      - name: "Démarre MariaDB"
        run: |
          docker network create configurationglpiauto-phpstan
          docker run -d --name mariadb --network configurationglpiauto-phpstan \
            -e MARIADB_ROOT_PASSWORD=glpi_password \
            -e MARIADB_DATABASE=glpidb \
            -e MARIADB_USER=glpi_user \
            -e MARIADB_PASSWORD=glpi_password \
            mariadb:11

          echo "Attente que MariaDB accepte les connexions..."
          for i in $(seq 1 30); do
            docker exec mariadb mariadb-admin ping -h 127.0.0.1 -u root -pglpi_password --silent && break
            sleep 2
          done

      - name: "Démarre GLPI (auto-installation via l'image officielle)"
        run: |
          docker run -d --name glpi --network configurationglpiauto-phpstan \
            -e TIMEZONE=Europe/Paris \
            -e GLPI_DB_HOST=mariadb \
            -e GLPI_DB_PORT=3306 \
            -e GLPI_DB_NAME=glpidb \
            -e GLPI_DB_USER=glpi_user \
            -e GLPI_DB_PASSWORD=glpi_password \
            "$GLPI_IMAGE"

          echo "Attente que l'auto-installation de GLPI soit terminee..."
          ready=0
          for i in $(seq 1 60); do
            docker exec glpi test -f /var/glpi/config/config_db.php && { ready=1; break; }
            sleep 5
          done
          if [ "$ready" != "1" ]; then
            echo "::error::GLPI n'a jamais termine son auto-installation (config_db.php absent)."
            docker logs glpi --tail 200
            exit 1
          fi

      - name: "Copie le plugin dans le conteneur GLPI"
        run: |
          docker exec glpi mkdir -p /var/www/glpi/plugins/configurationglpiauto
          docker cp ./. glpi:/var/www/glpi/plugins/configurationglpiauto
          # cf. assetsign-glpi/.github/workflows/ci.yml : les fichiers copies appartiennent a root
          # (l'utilisateur du checkout), pas www-data (celui sous lequel tourne PHP dans ce
          # conteneur) -- sans ce chown, composer install echoue en tentant de creer des dossiers
          # dans vendor/.
          docker exec -u root glpi chown -R www-data:www-data /var/www/glpi/plugins/configurationglpiauto

      - name: "Installe les dépendances (composer, y compris PHPStan)"
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          docker exec glpi sh -c '
            export COMPOSER_HOME=/tmp/composer-home
            mkdir -p "$COMPOSER_HOME"
            cd /tmp
            php -r "copy(\"https://getcomposer.org/installer\", \"composer-setup.php\");"
            php composer-setup.php --install-dir=/tmp --filename=composer.phar
          '
          docker exec -e COMPOSER_AUTH="{\"github-oauth\":{\"github.com\":\"$GH_TOKEN\"}}" glpi sh -c '
            export COMPOSER_HOME=/tmp/composer-home
            cd /var/www/glpi/plugins/configurationglpiauto
            php /tmp/composer.phar install --no-interaction --prefer-dist --optimize-autoloader
          '

      - name: "Run PHPStan"
        run: |
          docker exec glpi sh -c "cd /var/www/glpi/plugins/configurationglpiauto && vendor/bin/phpstan analyse --no-progress --memory-limit=1G"

  php-cs-fixer:
    name: "PHP-CS-Fixer"
    runs-on: ubuntu-latest
    needs: ci

    steps:
      - name: "Checkout code"
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7 (SHA pinned, cf. Semgrep github-actions-mutable-action-tag)

      - name: "Setup PHP"
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # v2 (SHA pinned, cf. Semgrep github-actions-mutable-action-tag)
        with:
          php-version: "8.2"
          coverage: none
          tools: composer

      - name: "Install dependencies"
        run: |
          composer install --prefer-dist --no-progress --no-interaction

      - name: "Check code style"
        run: |
          vendor/bin/php-cs-fixer fix --dry-run --diff --allow-risky=yes

  locales:
    name: "Locale Completeness"
    runs-on: ubuntu-latest
    needs: ci
    # #174: catches new __()/_n() strings added to the code without being propagated to
    # locales/*.po — same failure mode that let ~25 strings (version tracking, inventory,
    # certificate/licence types, recurring tickets, RSS feeds, project statuses) sit untranslated
    # in 4 of 5 languages for several sprints before anyone noticed. Regenerates the .pot from the
    # current code (vendor/bin/extract-locales, already a dev dependency — same tool a human
    # contributor would run by hand, not a hand-rolled regex) and fails if any string it finds is
    # missing a translation in any of the 6 shipped languages.
    steps:
      - name: "Checkout code"
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7 (SHA pinned, cf. Semgrep github-actions-mutable-action-tag)

      - name: "Setup PHP"
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # v2 (SHA pinned, cf. Semgrep github-actions-mutable-action-tag)
        with:
          php-version: "8.2"
          coverage: none
          tools: composer

      - name: "Install dependencies"
        run: |
          composer install --prefer-dist --no-progress --no-interaction

      - name: "Install gettext"
        run: |
          sudo apt-get update -qq && sudo apt-get install -y --no-install-recommends gettext

      - name: "Regenerate .pot from current code"
        run: |
          vendor/bin/extract-locales
          # extract-locales' own last step overwrites en_GB.po from scratch via msginit (French
          # msgid copied as a placeholder msgstr for every string, wiping real English
          # translations) — undo that so en_GB.po is checked the same way as the other 4
          # languages: merged against the fresh .pot, not blindly regenerated.
          git checkout -- locales/en_GB.po

      - name: "Check every language has a translation for every extracted string"
        run: |
          status=0
          for lang in fr_FR en_GB de_DE es_ES it_IT pt_BR; do
            msgmerge --no-fuzzy-matching --quiet -o /tmp/merged_${lang}.po locales/${lang}.po locales/configurationglpiauto.pot
            missing=$(msgattrib --untranslated /tmp/merged_${lang}.po | grep -c '^msgid "[^"]' || true)
            if [ "$missing" -gt 0 ]; then
              echo "::error::${lang}.po is missing ${missing} translation(s) for strings present in the code"
              msgattrib --untranslated /tmp/merged_${lang}.po | grep '^msgid "[^"]'
              status=1
            fi
          done
          exit $status

  phpunit:
    name: "PHPUnit Tests"
    runs-on: ubuntu-latest
    needs: ci
    # No real GLPI here (plain shivammathur/setup-php) — explicitly pinned to phpunit.xml.dist
    # (tests/Unit only, pure PHP). tests/Integration/ needs a real, installed-and-activated GLPI
    # instance to exercise CommonDBTM::add()/real DB writes/rule evaluation against — that suite
    # runs inside the "ci" job above instead, which GLPI's official plugin-ci-workflows reusable
    # workflow already provides (auto-detects the bare phpunit.xml at repo root once it exists and
    # runs it from inside the real, plugin-activated container — confirmed by reading that
    # workflow's own source, not assumed). Two config files, two execution contexts, on purpose.
    steps:
      - name: "Checkout code"
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7 (SHA pinned, cf. Semgrep github-actions-mutable-action-tag)

      - name: "Setup PHP"
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # v2 (SHA pinned, cf. Semgrep github-actions-mutable-action-tag)
        with:
          php-version: "8.2"
          coverage: xdebug
          tools: composer

      - name: "Install dependencies"
        run: |
          composer install --prefer-dist --no-progress --no-interaction

      - name: "Run unit tests with coverage"
        run: |
          vendor/bin/phpunit -c phpunit.xml.dist --coverage-text --coverage-clover=coverage.xml

      - name: "Upload coverage to Codecov"
        uses: codecov/codecov-action@303a32d7a59b442fa8d48b6a1cc6825c09c847a5 # v7 (SHA pinned, cf. Semgrep github-actions-mutable-action-tag)
        with:
          file: ./coverage.xml
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false

  trivy:
    name: "Trivy Security Scan"
    runs-on: ubuntu-latest
    needs: ci

    steps:
      - name: "Checkout code"
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7 (SHA pinned, cf. Semgrep github-actions-mutable-action-tag)

      - name: "Run Trivy vulnerability scanner"
        # scanners vuln,secret,misconfig (etait vuln seul) : aligne sur la
        # couverture deja en place sur le plugin jumeau remise-glpi, qui
        # detectait des secrets/mauvaises configurations que ce depot ne
        # verifiait pas du tout jusqu'ici.
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0, pas @master (SHA pinned, cf. Semgrep github-actions-mutable-action-tag)
        with:
          scan-type: 'fs'
          scan-ref: '.'
          exit-code: '1'
          ignore-unfixed: true
          scanners: 'vuln,secret,misconfig'
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'

  semgrep:
    name: "Semgrep SAST"
    runs-on: ubuntu-latest
    needs: ci
    # Analyse par motifs (SAST) : distincte de Trivy ci-dessus, qui couvre
    # secrets/mauvaises configurations/vulnerabilites connues des dependances
    # mais ne detecte pas des patterns de code dangereux (ex: injection SQL,
    # usage risque d'une fonction PHP) - deux outils complementaires, pas
    # redondants. Aligne sur le plugin jumeau remise-glpi, qui a ce job
    # depuis le debut - absent ici jusqu'ici.
    #
    # Pas de `continue-on-error` : sur remise-glpi, l'avoir mis a rendu
    # pendant un temps son statut CI "vert" quoi que semgrep trouve - corrige
    # le 2026-08-12 sur ce depot-la (cf. son propre historique de commits).
    # Reproduit ici directement sans cette erreur plutot que de la repeter.
    container:
      image: semgrep/semgrep
    steps:
      - name: "Checkout code"
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7 (SHA pinned, cf. Semgrep github-actions-mutable-action-tag)
      - run: semgrep ci --config auto
        env:
          SEMGREP_APP_TOKEN: ${{ secrets.SEMGREP_APP_TOKEN }}

  dependabot:
    name: "Dependabot Security"
    runs-on: ubuntu-latest
    needs: ci

    steps:
      - name: "Checkout code"
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7 (SHA pinned, cf. Semgrep github-actions-mutable-action-tag)

      - name: "Setup PHP"
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # v2 (SHA pinned, cf. Semgrep github-actions-mutable-action-tag)
        with:
          php-version: "8.2"
          coverage: none
          tools: composer

      - name: "Install dependencies"
        run: |
          composer install --prefer-dist --no-progress --no-interaction

      - name: "Check for security vulnerabilities"
        run: |
          composer audit

  validation:
    name: "File Validation"
    runs-on: ubuntu-latest
    needs: ci

    steps:
      - name: "Checkout code"
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7 (SHA pinned, cf. Semgrep github-actions-mutable-action-tag)

      - name: "Validate JSON files"
        run: |
          find . -type f -name "*.json" -not -path "./vendor/*" -not -path "./.git/*" | xargs -I {} sh -c 'echo "Validating {}" && jq empty {} || exit 1'

      - name: "Install yamllint"
        run: |
          pip install --quiet yamllint

      - name: "Validate YAML files"
        run: |
          find . -type f \( -name "*.yml" -o -name "*.yaml" \) -not -path "./vendor/*" -not -path "./.git/*" | xargs -I {} sh -c 'echo "Validating {}" && yamllint -c .yamllint.yml {} || exit 1'

      - name: "Install xmllint"
        run: |
          sudo apt-get update -qq && sudo apt-get install -y --no-install-recommends libxml2-utils

      - name: "Validate XML files"
        run: |
          find . -type f -name "*.xml" -not -path "./vendor/*" -not -path "./.git/*" | xargs -I {} sh -c 'echo "Validating {}" && xmllint --noout {} || exit 1'

      - name: "Validate Composer JSON"
        run: |
          composer validate --strict

  quality-gate:
    name: "Quality Gate"
    runs-on: ubuntu-latest
    needs: [phpstan, php-cs-fixer, phpunit, trivy, semgrep, dependabot, validation, locales]

    steps:
      - name: "All checks passed"
        run: |
          echo "All quality checks have passed successfully!"
          echo "PHPStan: ✓"
          echo "PHP-CS-Fixer: ✓"
          echo "PHPUnit: ✓"
          echo "Trivy: ✓"
          echo "Semgrep: ✓"
          echo "Dependabot: ✓"
          echo "Validation: ✓"
